rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read and write access for the next 30 days
      allow read, write: if request.time < timestamp.now() + duration.time(720h);
    }
  }
}
