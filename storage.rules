rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // TEMPORARY: Allow all reads and writes for debugging purposes.
    // WARNING: This is insecure for production. 
    // Once the upload issue is resolved, revert to the commented-out rules below.
    match /{allPaths=**} {
      allow read, write: if true;
    }

    /*
    // SECURE RULES (for production)
    match /users/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    match /settings/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /students/{allPaths=**} {
        allow read: if true;
        allow write: if request.auth != null;
    }
    */
  }
}
