
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read on all files
    match /{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
