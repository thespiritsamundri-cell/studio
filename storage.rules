service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read and write access to all users.
      // This is for demonstration purposes.
      // For production, you should restrict access based on your app's needs.
      // e.g., allow read, write: if request.auth != null;
      allow read, write: if true;
    }
  }
}
